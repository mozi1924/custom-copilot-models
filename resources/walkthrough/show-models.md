Responses models should appear in the Copilot model picker as soon as the extension is active. If an API key is not configured yet, they show a warning icon until you run Responses: Set API Key.

If you do not see them right away, run Responses: Refresh Models from the Command Palette.
